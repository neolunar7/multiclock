# Covers .build/ plus the per-arch scratch paths used by the universal build.
.build*/
build/
.DS_Store
*.xcuserstate

# Generated by Tools/GenerateIcon.swift during build; the drawing code is the source
# of truth, so the rendered artwork doesn't belong in version control.
Resources/AppIcon.iconset/
Resources/AppIcon.icns
